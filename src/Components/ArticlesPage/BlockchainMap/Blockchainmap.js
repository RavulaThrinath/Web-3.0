import React from 'react'
import "../NFT/Nft.css"

const Blockchainmap = (props) => {
    return (
        <div>
        <h1 className="h1tag">
        {props.mainTitle}
      </h1>
        </div>
    )
}

export default Blockchainmap
